# 🚀 Инструкция по деплою на продакшн

## Обзор

Приложение развернуто на **Beget VPS** (155.212.209.24) и доступно по адресу:
- **https://dndgenerator.fun**
- **https://www.dndgenerator.fun**

---

## 📦 Что включает деплой

- **Frontend**: React + Vite (собран в статику)
- **Backend**: Node.js + Express + Socket.IO
- **Database**: PostgreSQL (локальная)
- **Web Server**: Nginx (раздача frontend + прокси для API)
- **Process Manager**: PM2 (автозапуск backend)
- **SSL**: Let's Encrypt (автоматическое продление)

---

## 🔄 Как деплоить изменения на прод

### Вариант 1: Быстрый деплой (рекомендуется)

Используйте готовый скрипт для автоматического обновления:

```bash
# 1. Закоммитьте и запушьте изменения
git add .
git commit -m "описание изменений"
git push origin main

# 2. Подключитесь к серверу
ssh root@155.212.209.24
# Пароль: dftTjv&Y5t1U

# 3. Запустите скрипт обновления
cd /var/www/dnd-character-generator
bash deploy/update.sh
```

Скрипт `update.sh` автоматически:
- ✅ Остановит PM2
- ✅ Скачает изменения с GitHub
- ✅ Пересоберет backend
- ✅ Пересоберет frontend
- ✅ Применит миграции БД (если есть)
- ✅ Перезапустит PM2
- ✅ Перезагрузит Nginx

---

### Вариант 2: Ручной деплой (пошагово)

Если нужен больший контроль или скрипт не работает:

#### Шаг 1: Закоммитьте изменения локально

```bash
# В корне проекта на вашем компьютере
git add .
git commit -m "описание изменений"
git push origin main
```

#### Шаг 2: Подключитесь к серверу

```bash
ssh root@155.212.209.24
# Пароль: dftTjv&Y5t1U
```

#### Шаг 3: Перейдите в папку проекта

```bash
cd /var/www/dnd-character-generator
```

#### Шаг 4: Скачайте изменения

```bash
git pull origin main
```

#### Шаг 5: Обновите backend

```bash
cd backend

# Установите новые зависимости (если добавляли)
npm install

# Пересоберите TypeScript
npm run build

# Вернитесь в корень проекта
cd ..
```

#### Шаг 6: Примените миграции БД (если есть)

```bash
cd backend

# Примените Prisma schema
npx prisma db push

# Если есть SQL миграции, примените их
sudo -u postgres psql -d dnd_generator -f prisma/migrations/ваш_файл.sql

cd ..
```

#### Шаг 7: Обновите frontend

```bash
# Установите новые зависимости (если добавляли)
npm install

# Пересоберите frontend с правильным base path
npm run build -- --base=/
```

#### Шаг 8: Перезапустите backend

```bash
pm2 restart dnd-backend
```

#### Шаг 9: Перезагрузите Nginx (опционально)

```bash
systemctl reload nginx
```

#### Шаг 10: Проверьте статус

```bash
# Проверьте статус PM2
pm2 status

# Посмотрите логи (если нужно)
pm2 logs dnd-backend --lines 50
```

---

## 🗃️ Работа с базой данных

### Применение новых миграций

Если вы изменили Prisma schema:

```bash
cd /var/www/dnd-character-generator/backend

# Применить схему
npx prisma db push --accept-data-loss

# Если есть SQL миграция
sudo -u postgres psql -d dnd_generator -f prisma/migrations/2025-01-28_название.sql
```

### Создание бэкапа БД (перед важными изменениями)

```bash
sudo -u postgres pg_dump dnd_generator > ~/backup_$(date +%Y%m%d_%H%M%S).sql
```

### Восстановление из бэкапа

```bash
sudo -u postgres psql -d dnd_generator < ~/backup_файл.sql
```

---

## 📊 Полезные команды

### PM2 (управление backend)

```bash
# Статус приложения
pm2 status

# Логи в реальном времени
pm2 logs dnd-backend

# Последние 100 строк логов
pm2 logs dnd-backend --lines 100

# Перезапуск
pm2 restart dnd-backend

# Остановка
pm2 stop dnd-backend

# Запуск (если остановлен)
pm2 start dnd-backend

# Информация о процессе
pm2 info dnd-backend
```

### Nginx (web сервер)

```bash
# Проверить конфигурацию
nginx -t

# Перезагрузить конфигурацию (без даунтайма)
systemctl reload nginx

# Перезапустить Nginx
systemctl restart nginx

# Статус
systemctl status nginx

# Посмотреть логи
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### PostgreSQL

```bash
# Подключиться к БД
sudo -u postgres psql -d dnd_generator

# Список таблиц (внутри psql)
\dt

# Выйти из psql
\q

# Статус PostgreSQL
systemctl status postgresql
```

### Системные команды

```bash
# Использование диска
df -h

# Использование памяти
free -h

# Загрузка CPU
htop

# Перезагрузка сервера (только если необходимо!)
reboot
```

---

## 🔐 Безопасность

### SSL сертификат

Сертификат обновляется автоматически. Проверить автообновление:

```bash
certbot renew --dry-run
```

Если нужно обновить вручную:

```bash
certbot renew
systemctl reload nginx
```

### Смена паролей

**Пароль БД** (рекомендуется сменить!):

```bash
# 1. Подключитесь к PostgreSQL
sudo -u postgres psql

# 2. Смените пароль
ALTER USER dnduser WITH PASSWORD 'ваш_новый_супер_сложный_пароль';
\q

# 3. Обновите .env в backend
nano /var/www/dnd-character-generator/backend/.env
# Измените DATABASE_URL и DIRECT_URL

# 4. Перезапустите backend
pm2 restart dnd-backend
```

**Пароль root SSH**:

```bash
passwd
```

---

## 🐛 Troubleshooting (решение проблем)

### Backend не запускается

```bash
# Посмотрите логи
pm2 logs dnd-backend --lines 100

# Проверьте, не занят ли порт 3001
lsof -i :3001

# Перезапустите с очисткой
pm2 delete dnd-backend
cd /var/www/dnd-character-generator/backend
pm2 start npm --name dnd-backend -- start
pm2 save
```

### Frontend показывает белый экран

```bash
# Проверьте, что файлы собраны
ls -la /var/www/dnd-character-generator/dist/

# Пересоберите frontend
cd /var/www/dnd-character-generator
npm run build -- --base=/

# Проверьте конфигурацию Nginx
nginx -t

# Перезагрузите Nginx
systemctl reload nginx
```

### Ошибка "502 Bad Gateway"

Означает, что Nginx не может подключиться к backend:

```bash
# Проверьте статус backend
pm2 status

# Если backend остановлен - запустите
pm2 start dnd-backend

# Посмотрите логи
pm2 logs dnd-backend
```

### Ошибки БД

```bash
# Проверьте статус PostgreSQL
systemctl status postgresql

# Перезапустите PostgreSQL (если нужно)
systemctl restart postgresql

# Проверьте подключение
sudo -u postgres psql -d dnd_generator -c "SELECT 1;"
```

### WebSocket не работает

```bash
# Проверьте конфигурацию Nginx для Socket.IO
cat /etc/nginx/sites-available/dnd-generator | grep -A 10 "socket.io"

# Перезапустите backend и Nginx
pm2 restart dnd-backend
systemctl reload nginx
```

---

## 📁 Важные файлы и пути

| Что | Путь |
|-----|------|
| Проект | `/var/www/dnd-character-generator/` |
| Frontend (собранный) | `/var/www/dnd-character-generator/dist/` |
| Backend | `/var/www/dnd-character-generator/backend/` |
| Backend .env | `/var/www/dnd-character-generator/backend/.env` |
| Конфигурация Nginx | `/etc/nginx/sites-available/dnd-generator` |
| SSL сертификаты | `/etc/letsencrypt/live/dndgenerator.fun/` |
| Логи PM2 | `/root/.pm2/logs/` |
| Логи Nginx | `/var/log/nginx/` |
| Скрипты деплоя | `/var/www/dnd-character-generator/deploy/` |

---

## 🔗 Полезные ссылки

- **Сайт**: https://dndgenerator.fun
- **GitHub**: https://github.com/devdenneg/dnd-character-generator
- **Сервер**: 155.212.209.24

---

## 📞 Учетные данные (храните в безопасности!)

| Сервис | Логин | Пароль | Примечание |
|--------|-------|--------|------------|
| SSH | root | `dftTjv&Y5t1U` | Beget VPS |
| PostgreSQL | dnduser | `DnDSecure2026Pass` | Локальная БД |
| Домен | - | - | Beget панель |

⚠️ **ВАЖНО**: Смените пароль БД после первого входа!

---

## ✅ Checklist для каждого деплоя

- [ ] Протестировали изменения локально
- [ ] Закоммитили все изменения
- [ ] Запушили в main ветку
- [ ] Подключились к серверу
- [ ] Скачали изменения (git pull)
- [ ] Обновили зависимости (если добавляли)
- [ ] Пересобрали backend
- [ ] Применили миграции БД (если есть)
- [ ] Пересобрали frontend
- [ ] Перезапустили PM2
- [ ] Проверили логи на ошибки
- [ ] Открыли сайт и протестировали

---

**Удачного деплоя! 🚀**
