# Настройка GitHub Secrets для автоматического деплоя

## Шаги настройки:

### 1. Откройте настройки репозитория
Перейдите в ваш репозиторий на GitHub:
```
https://github.com/devdenneg/dnd-character-generator
```

### 2. Добавьте секреты
1. Нажмите **Settings** (в верхнем меню репозитория)
2. В левом меню выберите **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**

### 3. Добавьте следующие секреты:

#### Секрет 1: SSH_HOST
- **Name**: `SSH_HOST`
- **Secret**: `155.212.209.24`
- Нажмите **Add secret**

#### Секрет 2: SSH_USER
- **Name**: `SSH_USER`
- **Secret**: `root`
- Нажмите **Add secret**

#### Секрет 3: SSH_PASSWORD
- **Name**: `SSH_PASSWORD`
- **Secret**: `dftTjv&Y5t1U`
- Нажмите **Add secret**

---

## ✅ После настройки:

Каждый раз, когда вы делаете `git push` в ветку `main`, GitHub Actions автоматически:

1. 📥 Подключится к вашему серверу Beget
2. 🔄 Выполнит `git pull origin main`
3. 📦 Установит зависимости `npm install`
4. 🔨 Соберет проект `npm run build -- --base=/`
5. 🔄 Перезагрузит Nginx `systemctl reload nginx`
6. ✅ Сайт обновится автоматически!

---

## 🔍 Как проверить:

После пуша перейдите в:
```
https://github.com/devdenneg/dnd-character-generator/actions
```

Там вы увидите статус деплоя в реальном времени.

---

## 🚀 Использование:

Теперь просто делайте:
```bash
git add .
git commit -m "ваше сообщение"
git push origin main
```

И через 1-2 минуты изменения автоматически появятся на **https://dndgenerator.fun** 🎉
