# D&D Character Generator - Backend

Backend API для управления пользователями и персонажами D&D 5e.

## 🗄️ Структура баз данных

- **Production (Render)**: `.env` → Neon БД для продакшена
- **Local Development**: `.env.local` → отдельная Neon БД для разработки

## 🚀 Запуск локально

```bash
cd backend

# Установить зависимости
npm install

# Запустить сервер (использует .env.local автоматически)
npm run dev
```

Сервер запустится на **http://localhost:3001**

## 🔧 Технические детали

### Neon Serverless Driver
Используется **@prisma/adapter-neon** вместо стандартного PostgreSQL драйвера для обхода проблем с SSL сертификатами на macOS.

### Автоматический выбор .env
- Локально: загружается `.env.local` (если существует)
- Production: загружается `.env`

Логика в `src/index.ts` и `prisma.config.ts`.

## 📡 API Endpoints

### Auth
- `POST /api/auth/register` - Регистрация (с выбором роли player/master)
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход
- `GET /api/auth/me` - Текущий пользователь

### Characters (требуется авторизация)
- `GET /api/characters` - Список персонажей
- `POST /api/characters` - Создать персонажа
- `GET /api/characters/:id` - Получить персонажа
- `PUT /api/characters/:id` - Обновить персонажа
- `DELETE /api/characters/:id` - Удалить персонажа

## 🐛 Troubleshooting

### Ошибка P1011: bad certificate format
Решается использованием Neon serverless driver (`@prisma/adapter-neon`), который уже настроен в проекте.
